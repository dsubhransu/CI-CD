apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: q
  labels:
    app: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:stable
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: q
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 31323
    protocol: TCP
    name: nginx
  selector:
    app: nginx
